from setuptools import setup

setup(
    name='bannedtek',
    version='1.0',
    description='detect banned function',
    author='ydhemtek',
    packages=[''],
    install_requires=[
        'termcolor',
        'pyfiglet',
    ],
)
